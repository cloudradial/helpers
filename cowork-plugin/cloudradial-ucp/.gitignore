# Dependencies & build output
node_modules/
azure-mcp-server/dist/

# Bundled MCP server runtime state (created on first launch)
mcp-server/.install-complete
mcp-server/.env
mcp-server/*.log

# Azure Function local secrets
azure-mcp-server/local.settings.json

# Local credential config (created by setup wizard)
cloudradial-config.json

# Large reference file (api-reference.md covers what's needed)
references/swagger.json

# Build artifacts
*.plugin

# IDE & OS
.DS_Store
.vscode/
infra/
